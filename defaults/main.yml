---
docker_key_url: "https://download.docker.com/linux/{{ ansible_facts['distribution'] | lower }}/gpg"
docker_key_dir: /usr/share/keyrings
docker_key_file: docker.asc
docker_key_path: "{{ docker_key_dir | regex_replace('/+$', '') }}/{{ docker_key_file }}"
docker_pkg_state: present
